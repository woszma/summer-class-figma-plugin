{
  "name": "summer-class-figma-plugin",
  "version": "1.0.0",
  "description": "暑期班課程卡生成器 — Notion to Figma sync plugin",
  "scripts": {
    "build": "tsc --noEmit false",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.90.0",
    "typescript": "^5.3.0"
  }
}
